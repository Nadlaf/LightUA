# Development: leave empty so /api/* requests stay same-origin and are served by
# the dev fixture API (dev/fixture-api.ts) from fixtures/.
# To develop against the real hosted backend instead, set VITE_API_URL in
# .env.development.local (gitignored), e.g. VITE_API_URL=http://<host>:5000
VITE_API_URL=
